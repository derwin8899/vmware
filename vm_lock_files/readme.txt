Bug in backup software has a tendency to leave orphaned lock files behind and this can cause the VM to fail a start operation. The reporting script finds any lock files on the VMFS datastores and provides an output file. The second script uses the output file as input and verifies the location of the lock file and provides the user with the option to remove it from the VM directory. 
